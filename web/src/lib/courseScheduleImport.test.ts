import { describe,expect,it } from "vitest";import { parseCourseSchedule,scheduleCandidatesToTracker } from "./courseScheduleImport";import type { TrackerItem } from "./types";
describe("course schedule intake",()=>{it("extracts dated course work conservatively",()=>{const rows=parseCourseSchedule("2026-08-14,Renal Physiology,Lecture\n2026-08-14,Renal DLA,DLA\n2026-08-20,IMCQ 2,Assessment");expect(rows.map(row=>[row.date,row.kind])).toEqual([["2026-08-14","Lecture"],["2026-08-14","DLA"],["2026-08-20","Assessment"]]);expect(scheduleCandidatesToTracker(rows,"T1/Cardio")[2].assessmentDate).toBe("2026-08-20");});it("marks exact duplicates and preserves likely duplicates for review",()=>{const existing:TrackerItem[]=[{id:"x",path:"T1",label:"Renal Physiology",kind:"Lecture",passes:0,ankiPasses:0,yield:"none",updated:"2026-08-01",note:"Scheduled 2026-08-14"},{id:"y",path:"T1",label:"Cardiac Lab",kind:"Lab",passes:0,ankiPasses:0,yield:"none",updated:"2026-08-01"}];const rows=parseCourseSchedule("2026-08-14,Renal Physiology,Lecture\n2026-08-15,Cardiac Lab,Lab",existing);expect(rows[0].duplicate).toBe("exact");expect(rows[0].selected).toBe(false);expect(rows[1].duplicate).toBe("likely");});});
